<div class="error-message">
    <!-- only report the error message if the object errorMessage is created -->
    <% if (locals.errorMessage != null) { %>
        <!-- report the error message -->
        <%= errorMessage %> 
    <% } %>
</div>
    
